{
  "name": "rx-from-events",
  "version": "1.0.0",
  "description": "rxjs fromEvent but for multiple events",
  "main": "dist/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node ./dist/index.js",
    "build": "tsc",
    "serve": "nodemon --exec ./node_modules/.bin/ts-node -- ./src/index.ts"
  },
  "author": "Brandon Blaylock (brandon@null.pub)",
  "license": "MPL-2.0",
  "dependencies": {
    "rxjs": "^5.4.3"
  },
  "devDependencies": {
    "@types/node": "^8.0.24",
    "ts-node": "^3.3.0",
    "nodemon": "^1.11.0",
    "typescript": "^2.4.2"
  }
}
